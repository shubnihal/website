/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "https://www.google.com",
      "https://www.istockphoto.com/",
    ], // your image host domain
  },
};

export default nextConfig;
